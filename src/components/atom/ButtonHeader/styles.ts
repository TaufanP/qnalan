import { StyleSheet } from "react-native";

const size = 54;

const styles = () =>
  StyleSheet.create({
    container: {
      height: size,
      width: size,
      justifyContent: "center",
      alignItems: "center",
    },
  });

export default styles;
