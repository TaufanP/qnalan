type StackParamsList = {
  AUTH_SCREEN: {
    isRegister: boolean;
  };
  ROOM_CHAT_SCREEN: {
    partnerId: string;
    messageId: string;
    roomId: string;
  };
};

export default StackParamsList;
