const fancyType = {
  success: "success",
  warning: "warning",
  failed: "failed",
};

const defaultState = {
  visible: false,
  type: fancyType.success,
  msg: "Put your message here",
};

export default { defaultState, fancyType };
