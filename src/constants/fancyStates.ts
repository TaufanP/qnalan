const fancyType = {
  failed: "failed",
  success: "success",
  warning: "warning",
};

const defaultState = {
  msg: "Put your message here",
  type: fancyType.success,
  visible: false,
};

export default { defaultState, fancyType };
