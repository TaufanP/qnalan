const globalConstants = {
  appName: "Piaraanku",
  regexEmail: /^([a-zA-Z0-9_\-\.]+)@([a-zA-Z0-9_\-\.]+)\.([a-zA-Z]{2,5})$/,
  regexUsername: /^([a-z0-9A-Z]|[-._](?![-._])){5,15}$/, //"Username requires 5 up to 15 characters. Only could contains alphanumeric characters and one underscore or dash or dot.
  headerHeight: 54,
};

export default globalConstants;
